# 모일곳 수정 가이드 (디자인·개발팀용)

main 브랜치에 올리면 1분 안에 https://moilgot.vercel.app 에 자동 반영돼요.
실제 친구들이 쓰는 사이트라서, 아래 순서대로만 하면 사고 없이 고칠 수 있어요.

## 0. 처음 한 번

- 메일로 온 GitHub 초대(luispark0217/moilgot)를 **수락**해야 수정할 수 있어요.

## 1. 고치기 전

- 단톡방에 **"지금 index.html 수정 중"** 이라고 남기기. 앱이 거의 index.html 한 파일이라, 두 명이 동시에 고치면 나중에 저장한 사람이 앞사람 수정을 덮어써요.
- 끝나면 **"수정 끝"** 남기기.

## 2. 어디를 고치나

| 바꾸고 싶은 것 | 위치 (index.html) |
|---|---|
| 색상 (라임, 배경, 글자색 등) | 맨 위 `<style>` 안 `:root{ --lime:... }` 부분. 다크모드 색은 바로 아래 `@media (prefers-color-scheme: dark)` |
| 글꼴·크기·간격·모서리 | `<style>` 안 각 클래스 (`.btn`, `.chip`, `.card`, `.sheet` …) |
| 화면 문구·배치 | `V.home=`, `V.s1=` … 처럼 `V.화면이름=` 으로 시작하는 부분 (Ctrl/Cmd+F로 검색) |
| 버튼을 눌렀을 때 동작 | `const A={` 아래 (개발 담당과 상의 후) |

화면 이름: `home` 홈 · `s1~s4` 모임 만들기 1~4단계 · `result` 추천 결과 · `places` 장소 리스트 · `done` 약속 확정 · `g_land`/`g_wait` 초대받은 친구 화면 · `today` 오늘의 약속 · `race` 도착 레이스 · `chat` 채팅 · `roulette` 벌칙 룰렛 · `records` 기록 · `my` 마이

## 3. 절대 건드리지 말 것

- `const CONFIG={ ... }` 블록 (지도 키, DB 주소). 바뀌면 지도·실시간 기능이 멈춰요.
- `api/transit.js`, `vercel.json`, `supabase/schema.sql` (민석이 관리)
- `data-a="..."`, `data-v="..."`, `id="..."` 같은 속성 값. 버튼 동작이 여기에 연결돼 있어요. 글자·스타일만 바꾸고 속성은 그대로 두세요.
- `${ ... }` 안의 내용. 화면에 값을 넣는 코드예요.

## 4. 올리는 방법 (GitHub 웹)

**작은 수정 (색, 문구, 여백)** → 바로 main에
1. 저장소에서 `index.html` 열기 → 오른쪽 위 연필(✏️) 아이콘
2. 수정 → **Commit changes...** → 무엇을 바꿨는지 한 줄 적기 → **Commit directly to the main branch** → Commit
3. 1~2분 뒤 사이트 새로고침해서 확인

**큰 수정 (화면 구조 변경)** → 미리보기 링크로 먼저 확인
1. 위와 같이 수정 → Commit 창에서 **Create a new branch** 선택 → Propose changes → Create pull request
2. 잠시 뒤 PR 아래에 Vercel 봇이 **Preview 링크**를 달아줘요. 실제 사이트는 그대로고, 그 링크에서만 새 버전이 보여요.
3. 폰으로 확인하고 괜찮으면 **Merge pull request** → 실제 사이트 반영

## 5. 올린 뒤 꼭 확인 (폰으로 2분)

- [ ] 홈 → 새 모임 만들기 → 1~4단계 넘어가기
- [ ] 내 출발지 검색이 되고 지도가 뜨는지
- [ ] 초대 링크를 다른 폰(또는 시크릿 창)으로 열어 친구 화면이 뜨는지
- [ ] 바꾼 화면이 다크모드에서도 글자가 잘 보이는지

## 6. 문제가 생기면

- 사이트가 하얗게 나오거나 버튼이 안 먹으면: 방금 커밋을 되돌리기
  - GitHub 저장소 → Commits → 방금 커밋 → 수정 전 내용으로 다시 커밋하거나,
  - 민석에게 말하면 Vercel에서 **이전 배포로 즉시 되돌리기(Instant Rollback)** 가능
- 1~2분이 지나도 사이트가 안 바뀌면: 민석에게 알려주세요 (Vercel 배포 승인이 필요할 수 있어요)

## 참고

- 디자인 요청 목록: 디자인팀에 보낸 `모일곳_디자인요청_0925.zip`
- 운영 정보(계정·키 위치): 민석이 관리하는 운영 문서
